import AdminLoginClient from '../../../components/AdminLoginClient';

export const metadata = { title: 'Admin Login — Zaxis Studio' };

export default function AdminLogin() {
  return (
    <section>
      <h1 className="page-title">Admin Login</h1>
      <AdminLoginClient />
    </section>
  );
}
